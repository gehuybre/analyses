---
kind: workflow
id: URL-SYNC-ARCHITECTURE
title: URL-Synced State Architecture
owner: Unknown
status: reference
trigger: informational
slug: url-sync-architecture
parent: WF-url-synced-state-implementation.md
created: 2026-01-22
tags: [architecture, diagram, state-management]
files:
  - embuild-analyses/src/lib/stores/embed-filters-store.ts
  - docs/workflows/WF-url-synced-state-implementation.md
last_reviewed: 2026-01-25
---

# URL-Synced State Architecture

Visual representation of how URL-synced state management works.

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         Browser URL                             │
│  /analyses/faillissementen/?year=2024&sector=F&province=10000  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ ① loadFromUrl() on mount
                         │ ② syncToUrl() on change
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Zustand Store (Global)                       │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ State:                                                     │ │
│  │  - selectedYear: 2024                                     │ │
│  │  - selectedSector: 'F'                                    │ │
│  │  - selectedProvince: '10000'                              │ │
│  │  - currentView: 'chart'                                   │ │
│  │  - timeRange: 'monthly'                                   │ │
│  │  - ... (40+ filter properties)                            │ │
│  └───────────────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ Actions:                                                   │ │
│  │  - setYear(year) → update state → syncToUrl()            │ │
│  │  - setSector(sector) → update state → syncToUrl()        │ │
│  │  - setProvince(code) → update state → syncToUrl()        │ │
│  │  - loadFromUrl() → read URL → update state               │ │
│  └───────────────────────────────────────────────────────────┘ │
└──────┬────────────┬────────────┬────────────┬──────────────────┘
       │            │            │            │
       │ Subscribe  │ Subscribe  │ Subscribe  │ Subscribe
       ▼            ▼            ▼            ▼
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ Dashboard│ │ Filter   │ │ Export   │ │  Embed   │
│Component │ │Components│ │ Buttons  │ │ Component│
└──────────┘ └──────────┘ └──────────┘ └──────────┘
```

## Data Flow: User Changes Filter

```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: User selects year "2024" in dropdown                    │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Component calls setYear(2024)                           │
│                                                                  │
│   const setYear = useEmbedFilters((state) => state.setYear)    │
│   setYear(2024)  // ← User action                              │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: Store updates state                                     │
│                                                                  │
│   setYear: (year) => {                                          │
│     set({ selectedYear: year, lastUpdated: Date.now() })       │
│     get().syncToUrl()  // ← Automatic!                         │
│   }                                                              │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 4: syncToUrl() updates browser URL                         │
│                                                                  │
│   const params = new URLSearchParams()                          │
│   params.set('year', '2024')  // ← Add to URL                  │
│   window.history.replaceState({}, '', `?${params}`)            │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 5: All subscribed components re-render                     │
│                                                                  │
│   - Dashboard shows new filtered data                           │
│   - ExportButtons generates embed with year=2024               │
│   - Charts update with filtered data                            │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow: Page Load with URL Parameters

```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: User navigates to URL with filters                      │
│   /analyses/faillissementen/?year=2024&sector=F&province=10000 │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Dashboard component mounts                              │
│                                                                  │
│   export function FaillissementenDashboard() {                  │
│     useInitializeFilters()  // ← Load from URL                 │
│     ...                                                          │
│   }                                                              │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: loadFromUrl() reads URL parameters                      │
│                                                                  │
│   loadFromUrl: () => {                                          │
│     const params = new URLSearchParams(window.location.search) │
│     const updates = {                                           │
│       selectedYear: params.get('year'),    // → 2024           │
│       selectedSector: params.get('sector'), // → 'F'           │
│       selectedProvince: params.get('province') // → '10000'    │
│     }                                                            │
│     set(updates)  // ← Update store state                      │
│   }                                                              │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 4: Components render with URL state                        │
│                                                                  │
│   - Year selector shows "2024"                                  │
│   - Sector selector shows "Construction (F)"                    │
│   - Province selector shows "Antwerpen"                         │
│   - Data filtered accordingly                                   │
└─────────────────────────────────────────────────────────────────┘
```

## Component Integration Pattern

### Dashboard Component

```
┌─────────────────────────────────────────────────────────────────┐
│                 FaillissementenDashboard.tsx                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  import { useEmbedFilters, useInitializeFilters }              │
│                                                                  │
│  export function FaillissementenDashboard() {                   │
│    useInitializeFilters()  // ← Load from URL on mount         │
│                                                                  │
│    // Subscribe to specific state (granular)                    │
│    const selectedYear = useEmbedFilters(s => s.selectedYear)   │
│    const setYear = useEmbedFilters(s => s.setYear)             │
│    const selectedSector = useEmbedFilters(s => s.selectedSector)│
│    const setSector = useEmbedFilters(s => s.setSector)         │
│                                                                  │
│    return (                                                      │
│      <div>                                                       │
│        <YearSelector                                             │
│          value={selectedYear}                                   │
│          onChange={setYear}  // ← Auto-syncs to URL            │
│        />                                                        │
│        <SectorSelector                                           │
│          value={selectedSector}                                 │
│          onChange={setSector}  // ← Auto-syncs to URL          │
│        />                                                        │
│        <Chart year={selectedYear} sector={selectedSector} />   │
│      </div>                                                      │
│    )                                                             │
│  }                                                               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### ExportButtons Component

```
┌─────────────────────────────────────────────────────────────────┐
│                    ExportButtons.tsx                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  import { useEmbedFilters } from '@/lib/stores/embed-filters'  │
│                                                                  │
│  export function ExportButtons({ slug, sectionId, viewType }) { │
│    const toQueryString = useEmbedFilters(s => s.toQueryString) │
│                                                                  │
│    const getEmbedCode = () => {                                 │
│      // Get ALL current filters as query string                 │
│      const filters = toQueryString()                            │
│      // → "year=2024&sector=F&province=10000&view=chart"       │
│                                                                  │
│      const embedUrl =                                           │
│        `${baseUrl}/embed/${slug}/${sectionId}/?${filters}`     │
│                                                                  │
│      return `<iframe src="${embedUrl}" ...></iframe>`          │
│    }                                                             │
│                                                                  │
│    // No need to track embedParams manually!                    │
│    // Store automatically includes all active filters           │
│  }                                                               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## State Subscription Patterns

### Pattern 1: Full Store (Not Recommended)

```tsx
// ❌ Bad - Re-renders on ANY state change
const store = useEmbedFilters()
const selectedYear = store.selectedYear
```

### Pattern 2: Selective Subscription (Good)

```tsx
// ✅ Good - Only re-renders when selectedYear changes
const selectedYear = useEmbedFilters((state) => state.selectedYear)
const setYear = useEmbedFilters((state) => state.setYear)
```

### Pattern 3: Specialized Hooks (Best)

```tsx
// ✅ Best - Optimized for specific filter groups
const { selectedYear, setYear } = useTimeFilters()
const { selectedRegion, setRegion } = useGeoFilters()
const { currentView, setView } = useViewState()
```

### Pattern 4: Multiple Related Filters (Good)

```tsx
// ✅ Good - Single subscription for related state
const { selectedYear, selectedSector, selectedProvince } = useEmbedFilters((state) => ({
  selectedYear: state.selectedYear,
  selectedSector: state.selectedSector,
  selectedProvince: state.selectedProvince,
}))
```

## URL Parameter Mapping

```
┌────────────────────────────────────────────────────────────────┐
│                   Store State Property                         │
└──────────────────────┬─────────────────────────────────────────┘
                       │
                       │ Maps to (shorter)
                       ▼
┌────────────────────────────────────────────────────────────────┐
│                   URL Query Parameter                          │
└────────────────────────────────────────────────────────────────┘

Examples:
  selectedYear        →  year         (?year=2024)
  selectedQuarter     →  q            (?q=3)
  selectedSector      →  sector       (?sector=F)
  selectedProvince    →  province     (?province=10000)
  currentView         →  view         (?view=map)
  timeRange           →  range        (?range=monthly)
  showMovingAverage   →  ma           (?ma=1)
```

## Embed Flow: From User Action to Shareable URL

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. User visits page and applies filters                         │
│    /analyses/faillissementen/                                   │
│    Selects: Year=2024, Sector=F, View=Map                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. Store auto-updates URL                                       │
│    /analyses/faillissementen/?year=2024&sector=F&view=map      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. User clicks "Embed" button                                   │
│    ExportButtons reads current URL state via toQueryString()    │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. Generated embed code includes ALL filters                    │
│    <iframe                                                       │
│      src="/embed/faillissementen/evolutie/                      │
│           ?year=2024&sector=F&view=map"                         │
│    ></iframe>                                                    │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. Embed renders with exact filtered state                      │
│    EmbedClient calls loadFromUrl()                              │
│    Shows data filtered for: 2024, Construction, Map view        │
│    ✅ Perfect accuracy!                                         │
└─────────────────────────────────────────────────────────────────┘
```

## Migration Comparison

### Before: Manual State Management

```
┌────────────────────────────────────────────────────────────────┐
│                     Component State                            │
│  const [selectedYear, setSelectedYear] = useState(2024)       │
│  const [selectedSector, setSelectedSector] = useState('F')    │
│  const [selectedProvince, setSelectedProvince] = useState()   │
├────────────────────────────────────────────────────────────────┤
│                     Manual Tracking                            │
│  <ExportButtons                                                 │
│    embedParams={{                                              │
│      selectedYear,      ← Must manually add                   │
│      selectedSector,    ← Must manually add                   │
│      selectedProvince   ← Must manually add (often forgotten!)│
│    }}                                                           │
│  />                                                             │
├────────────────────────────────────────────────────────────────┤
│                     URL State                                  │
│  ❌ Not synced - filters lost on refresh                      │
│  ❌ Not shareable - URL doesn't include filters               │
│  ❌ Embed mismatch - may not include all filters              │
└────────────────────────────────────────────────────────────────┘
```

### After: URL-Synced State Management

```
┌────────────────────────────────────────────────────────────────┐
│                   Centralized Store                            │
│  const selectedYear = useEmbedFilters(s => s.selectedYear)    │
│  const setYear = useEmbedFilters(s => s.setYear)              │
│  const selectedSector = useEmbedFilters(s => s.selectedSector)│
│  const setSector = useEmbedFilters(s => s.setSector)          │
├────────────────────────────────────────────────────────────────┤
│                   Automatic Tracking                           │
│  <ExportButtons                                                 │
│    // No embedParams needed!                                   │
│    // Store auto-reads from URL                                │
│  />                                                             │
├────────────────────────────────────────────────────────────────┤
│                     URL State                                  │
│  ✅ Auto-synced - filters persist on refresh                  │
│  ✅ Shareable - URL includes all filters                      │
│  ✅ Perfect embeds - always matches user view                 │
└────────────────────────────────────────────────────────────────┘
```

## Performance Optimization

### Selective Re-renders

```
┌─────────────────────────────────────────────────────────────────┐
│                      Store State                                │
│  { selectedYear: 2024, selectedSector: 'F', currentView: ...}  │
└───────┬──────────────────┬──────────────────┬───────────────────┘
        │                  │                  │
        │ Subscribe to     │ Subscribe to     │ Subscribe to
        │ selectedYear     │ selectedSector   │ currentView
        │ only             │ only             │ only
        ▼                  ▼                  ▼
┌─────────────┐   ┌─────────────┐   ┌─────────────┐
│YearSelector │   │   Sector    │   │  ViewTabs   │
│             │   │  Selector   │   │             │
│Re-renders   │   │No re-render │   │No re-render │
│when year    │   │when year    │   │when year    │
│changes      │   │changes      │   │changes      │
└─────────────┘   └─────────────┘   └─────────────┘
```

Only components subscribed to changed state re-render!

## Redux DevTools Integration

```
┌─────────────────────────────────────────────────────────────────┐
│                   Redux DevTools (Browser)                      │
├─────────────────────────────────────────────────────────────────┤
│  Store: EmbedFilters                                            │
│                                                                  │
│  State Tree:                                                     │
│    ├─ selectedYear: 2024                                        │
│    ├─ selectedSector: "F"                                       │
│    ├─ selectedProvince: "10000"                                 │
│    ├─ currentView: "chart"                                      │
│    └─ ... (all 40+ properties)                                  │
│                                                                  │
│  Actions Log:                                                    │
│    1. loadFromUrl                                               │
│    2. setYear (2024)                                            │
│    3. setSector ("F")                                           │
│    4. setProvince ("10000")                                     │
│    5. setView ("chart")                                         │
│                                                                  │
│  Features:                                                       │
│    ⏮ Time-travel debugging (go back to any action)             │
│    📸 Export/import state snapshots                             │
│    🔍 Inspect state diff between actions                        │
│    ⚡ Dispatch actions manually                                  │
└─────────────────────────────────────────────────────────────────┘
```

## Error Handling & Validation

```
┌─────────────────────────────────────────────────────────────────┐
│                     URL Parameter Input                         │
│  ?year=invalid&sector=XYZ&province=99999                        │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   deserializeValue()                            │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ year: parseInt('invalid') → NaN → null (safe default)     │ │
│  │ sector: 'XYZ' → validate against NACE codes → fallback   │ │
│  │ province: '99999' → validate against geo-utils → fallback │ │
│  └───────────────────────────────────────────────────────────┘ │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Store State (Valid)                           │
│  {                                                               │
│    selectedYear: null,        // Invalid input → null           │
│    selectedSector: null,      // Unknown code → null            │
│    selectedProvince: null,    // Invalid code → null            │
│  }                                                               │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                 Component Sets Defaults                         │
│  useEffect(() => {                                              │
│    if (selectedYear === null) setYear(2024)  // Fallback       │
│  }, [selectedYear, setYear])                                    │
└─────────────────────────────────────────────────────────────────┘
```

Graceful degradation: Invalid URLs don't crash, fall back to defaults.

---

## Summary

This architecture provides:

✅ **Automatic URL Sync** - Every state change updates URL
✅ **Load from URL** - Page loads apply URL filters
✅ **Perfect Embeds** - Generated URLs include all filters
✅ **Type Safety** - Full TypeScript support
✅ **Performance** - Granular re-renders via selectors
✅ **DevTools** - Redux DevTools for debugging
✅ **Error Handling** - Graceful degradation on invalid input

**Simple to use, powerful under the hood.**
